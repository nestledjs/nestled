{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "ProjectConfig",
  "type": "object",
  "properties": {
    "overwritePrettier": {
      "type": "boolean",
      "description": "Generate .prettierrc file",
      "default": true,
      "x-prompt": "Do you want to generate a .prettierrc file? (Y/n)"
    },
    "generateEnv": {
      "type": "boolean",
      "description": "Generate .env.example file",
      "default": true,
      "x-prompt": "Do you want to generate a .env.example file? (Y/n)"
    },
    "generateDocker": {
      "type": "boolean",
      "description": "Generate Dockerfile and docker-compose.yml",
      "default": true,
      "x-prompt": "Do you want to generate Dockerfile and docker-compose.yml? (Y/n)"
    },
    "ignoreEnv": {
      "type": "boolean",
      "description": "Add .env in .gitignore",
      "default": true,
      "x-prompt": "Do you want to ignore .env file in .gitignore? (Y/n)"
    }
  },
  "required": []
}
