version: '3.8'
services:
  postgres:
    image: postgres
    container_name: <%= npmScope %>_postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: prisma
      POSTGRES_USER: prisma
      POSTGRES_PASSWORD: prisma
    volumes:
      - ./tmp/<%= npmScope %>_postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U $${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
  redis:
    image: redis:alpine
    container_name: <%= npmScope %>_redis
    ports:
      - '6379:6379'
    volumes:
      - ./tmp/<%= npmScope %>_redis:/data

  mailhog:
    image: mailhog/mailhog
    container_name: <%= npmScope %>_mailhog
    ports:
      - '1025:1025'
      - '8025:8025'
    restart: unless-stopped
